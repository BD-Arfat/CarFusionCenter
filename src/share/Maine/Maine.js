import React from 'react';
import { Outlet } from 'react-router-dom';

const Maine = () => {
    return (
        <div>
            <Outlet/>
        </div>
    );
};

export default Maine;